if ( confirm("Are you sure you want delete this assignment?") ) {
   var $form = $('<%= button_to("Delete", otwtranslation_destroy_assignment_path(@assignment_id), :remote => true, :method => :delete) %>')
   $form.appendTo('body')
   $form.submit()
}
