if ( confirm("Are you sure you want delete this assignment?") ) {
   var $form = $('<%= form_for(@assignment_id, :url => otwtranslation_destroy_assignment_path(@assignment_id), :html => { :method => :delete }, :remote => true ) do |f| %> <%= f.submit 'Delete' %> <% end %>')
   $form.appendTo('body')
   $form.submit()
}
