// replace form with "add assignee" button
$("form#otwtranslation_assignment_part").replaceWith(
  "<%= escape_javascript(
         render("otwtranslation/assignment_parts/add_assignee_button", 
                :assignment => @assignment)) %>")

// append new assignee to list
$("ol#otwtranslation_assignment_parts").append(  
  "<%= escape_javascript(render("otwtranslation/assignment_parts/part", 
           :assignment => @assignment, :part => @part)) %>")
