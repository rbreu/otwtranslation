if ( confirm("Are you sure you want delete this assignee?") ) {
   var $form = $('<%= button_to("Delete", otwtranslation_destroy_assignment_part_path(@part_id), :method => :delete) %>')
   $form.appendTo('body')
   $form.submit()
}
