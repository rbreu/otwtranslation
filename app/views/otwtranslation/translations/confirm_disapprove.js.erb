if ( confirm("Are you sure you want disapprove this translation?") ) {
   var $form = $j('<%= button_to("Disapprove", otwtranslation_disapprove_translation_path(@translation_id), :remote => true, :method => :post) %>')
   $form.appendTo('body')
   $form.submit()
}
