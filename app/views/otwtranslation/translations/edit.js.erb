// replace translation navigation bar on phrase page with edit form
$("li#translation<%= @translation.id %> .navigation").replaceWith(
  "<%= escape_javascript(render("otwtranslation/translations/edit_form", 
                                :translation => @translation)) %>")

// replace translation navigation bar on phrase page with edit form
$(".translations-show .translation.navigation").replaceWith(
  "<%= escape_javascript(render("otwtranslation/translations/edit_form", 
                                :translation => @translation)) %>")
