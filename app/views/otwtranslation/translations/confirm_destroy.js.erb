if ( confirm("Are you sure you want delete this translation?") ) {
   var $form = $('<%= form_for(@translation_id, :url => otwtranslation_destroy_translation_path(@translation_id), :html => { :method => :delete } ) do |f| %> <%= f.submit 'Delete' %> <% end %>')
   $form.appendTo('body')
   $form.submit()
}
