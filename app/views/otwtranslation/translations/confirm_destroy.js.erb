if ( confirm("Are you sure you want delete this translation?") ) {
   var $form = $('<%= button_to("Delete", otwtranslation_destroy_translation_path(@translation_id), :remote => false, :method => :delete) %>')
   $form.appendTo('body')
   $form.submit()
}
