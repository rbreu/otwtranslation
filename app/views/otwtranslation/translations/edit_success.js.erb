// Replace the edit form with the updated translation on translation list
$j("li#translation<%= @translation.id %>").html(
    "<%= escape_javascript(render("blurb", :translation => @translation)) %>")

// Replace the inline translation marked phrase 
$j("span#otwtranslation_phrase_<%= @translation.phrase_key %>").html(
    "<%= escape_javascript(@translation.label) %>")

// Replace the edit form on #show with navigation bar
$j("#edit_otwtranslation_translation_<%= @translation.id %>_form").replaceWith(
    "<%= escape_javascript(render("actions", :translation => @translation)) %>")

//Replace the translation label on #show
$j("dl.translation dd.label").html("<%= escape_javascript(@translation.label) %>")
