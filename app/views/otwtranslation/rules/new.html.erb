<!--Descriptive page name and system messages, descriptions, and instructions.-->
<h2><%= ts("Add Rule") %></h2>
<!--end descriptions-->

<!--Subnavigation, sorting and actions.-->
<!--end navigation-->

<!--main content-->

<%= form_for("otwtranslation_context_rule", @rule, :url => otwtranslation_post_rule_path) do |f| %>

<dl>
  <dt><%= f.label(:type, "Type:")%></dt>
  <dd><%= f.select(:type, @rule.RULE_TYPES, :selected => @rule.display_type) %> <%= f.submit "Set Type" %></dd>
</dl>

  <%= render :partial => "otwtranslation/rules/form",
             :locals => { :rule => @rule, :f => f } %>
  <p class="submit"><%= f.submit "Add rule" %></p>

<% end %>
