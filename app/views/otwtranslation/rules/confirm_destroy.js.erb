if ( confirm("Are you sure you want delete this rule?") ) {
   var $form = $('<%= form_for(@rule_id, :url => otwtranslation_destroy_rule_path(@rule_id), :html => { :method => :delete } ) do |f| %> <%= f.submit 'Delete' %> <% end %>')
   $form.appendTo('body')
   $form.submit()
}
