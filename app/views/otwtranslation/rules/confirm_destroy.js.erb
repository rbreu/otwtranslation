if ( confirm("Are you sure you want delete this rule?") ) {
   var $form = $j('<%= button_to("Delete", otwtranslation_destroy_rule_path(@rule_id), :remote => true, :method => :delete) %>')
   $form.appendTo('body')
   $form.submit()
}
