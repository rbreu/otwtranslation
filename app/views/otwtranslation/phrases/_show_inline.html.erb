<div class="show inline">

<!--BACK END: is this a heading? Please h4 if so-->
<p><%= @phrase.label %></p>

<!--BACK END: if this is a single form and not a list, let's drop the list structure-->
<ul class="translation navigation" id="add_translation">
  <li>
    <%= form_for(:translation, 
                 :url => otwtranslation_new_translation_path(@phrase), 
                 :html => { :method => :get },
                 :remote => true ) do |f| %>
      <%= f.submit 'Add translation' %>
    <% end %>
  </li>
</ul>

<%= render :partial => "otwtranslation/translations/translations_list", 
           :locals => { :translations => @translations } %>


<p class="navigation hide inline"><!--BACK END: assuming this is function tba-->Close</p>

</div>
